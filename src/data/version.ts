// Build/commit counter. Bumped automatically by .githooks/pre-commit on
// every commit, starting from 1 at this project's first commit. Displayed
// on the login page so anyone can tell which build they're looking at.
export const BUILD_VERSION = 8;
